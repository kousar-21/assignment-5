


// Main Card Button-1
document.getElementById('fix-mobile-btn').addEventListener('click',function(event){
    event.preventDefault();

    // Heading Text
    const mobileBtnIssue = document.getElementById('Mobile-btn-issue').innerText;

    // history
    const clearHistory = document.getElementById('history');

    const div = document.createElement('div')
    div.classList.add('bg-blue-200')
    div.classList.add('rounded-md')
    div.classList.add('p-3')
    div.classList.add('mb-3')
    div.innerHTML = `
    You have complete the task ${mobileBtnIssue} at 12:48 pm

    `
    clearHistory.appendChild(div)

    // Navber part
    const navberDigit = getAlertandValueByID("navber-digit");

    // head card part
    const headCards = getAlertandValueByID("head-card-digit");

    // calculation part
    const navberNumber = getCalculation2(navberDigit);
    document.getElementById('navber-digit').innerText = navberNumber;

    const headNumber = getCalculation(headCards);
    document.getElementById('head-card-digit').innerText = headNumber;

    // if( headCards === 1){
    //     alert("all task complete")
    // }


})





// Main Card Button-2
document.getElementById('btn-2').addEventListener('click',function(event){
    event.preventDefault();

    // Heading Text
    const mobileBtnIssue = document.getElementById('add-dark').innerText;

    // history
    const clearHistory = document.getElementById('history');

    const div = document.createElement('div')
    div.classList.add('bg-blue-200')
    div.classList.add('rounded-md')
    div.classList.add('p-3')
    div.classList.add('mb-3')
    div.innerHTML = `
    You have complete the task ${mobileBtnIssue} at 12:48 pm

    `
    clearHistory.appendChild(div)


    // Navber part
    const navberDigit = getAlertandValueByID("navber-digit");

    // head card part
    const headCards = getAlertandValueByID("head-card-digit");

    // calculation part
    const navberNumber = getCalculation2(navberDigit)
    document.getElementById('navber-digit').innerText = navberNumber;

    const headNumber = getCalculation(headCards)
    document.getElementById('head-card-digit').innerText = headNumber;

   
})



// Main Card Button-3
document.getElementById('btn-3').addEventListener('click',function(event){
    event.preventDefault();

    // Heading Text
    const mobileBtnIssue = document.getElementById('optimize-heading').innerText;

    // history
    const clearHistory = document.getElementById('history');

    const div = document.createElement('div')
    div.classList.add('bg-blue-200')
    div.classList.add('rounded-md')
    div.classList.add('p-3')
    div.classList.add('mb-3')
    div.innerHTML = `
    You have complete the task ${mobileBtnIssue} at 12:48 pm

    `
    clearHistory.appendChild(div)


    // Navber part
    const navberDigit = getAlertandValueByID("navber-digit");

    // head card part
    const headCards = getAlertandValueByID("head-card-digit");

    // calculation part
    const navberNumber = getCalculation2(navberDigit)
    document.getElementById('navber-digit').innerText = navberNumber;

    const headNumber = getCalculation(headCards)
    document.getElementById('head-card-digit').innerText = headNumber;

   
})



// Main Card Button-4
document.getElementById('btn-4').addEventListener('click',function(event){
    event.preventDefault();

    // Heading Text
    const mobileBtnIssue = document.getElementById('add-imoji').innerText;

    // history
    const clearHistory = document.getElementById('history');

    const div = document.createElement('div')
    div.classList.add('bg-blue-200')
    div.classList.add('rounded-md')
    div.classList.add('p-3')
    div.classList.add('mb-3')
    div.innerHTML = `
    You have complete the task ${mobileBtnIssue} at 12:48 pm

    `
    clearHistory.appendChild(div)


    // Navber part
    const navberDigit = getAlertandValueByID("navber-digit");

    // head card part
    const headCards = getAlertandValueByID("head-card-digit");

    // calculation part
    const navberNumber = getCalculation2(navberDigit)
    document.getElementById('navber-digit').innerText = navberNumber;

    const headNumber = getCalculation(headCards)
    document.getElementById('head-card-digit').innerText = headNumber;

   
})



// Main Card Button-5
document.getElementById('btn-5').addEventListener('click',function(event){
    event.preventDefault();

    // Heading Text
    const mobileBtnIssue = document.getElementById('open-ai').innerText;

    // history
    const clearHistory = document.getElementById('history');

    const div = document.createElement('div')
    div.classList.add('bg-blue-200')
    div.classList.add('rounded-md')
    div.classList.add('p-3')
    div.classList.add('mb-3')
    div.innerHTML = `
    You have complete the task ${mobileBtnIssue} at 12:48 pm

    `
    clearHistory.appendChild(div)


    // Navber part
    const navberDigit = getAlertandValueByID("navber-digit");

    // head card part
    const headCards = getAlertandValueByID("head-card-digit");

    // calculation part
    const navberNumber = getCalculation2(navberDigit)
    document.getElementById('navber-digit').innerText = navberNumber;

    const headNumber = getCalculation(headCards)
    document.getElementById('head-card-digit').innerText = headNumber;

   
})




// Main Card Button-6
document.getElementById('btn-6').addEventListener('click',function(event){
    event.preventDefault();

    // Heading Text
    const mobileBtnIssue = document.getElementById('improve-job').innerText;

    // history
    const clearHistory = document.getElementById('history');

    const div = document.createElement('div')
    div.classList.add('bg-blue-200')
    div.classList.add('rounded-md')
    div.classList.add('p-3')
    div.classList.add('mb-3')
    div.innerHTML = `
    You have complete the task ${mobileBtnIssue} at 12:48 pm

    `
    clearHistory.appendChild(div)


    // Navber part
    const navberDigit = getAlertandValueByID("navber-digit");

    // head card part
    const headCards = getAlertandValueByID("head-card-digit");

    // calculation part
    const navberNumber = getCalculation2(navberDigit)
    document.getElementById('navber-digit').innerText = navberNumber;

    const headNumber = getCalculation(headCards)
    document.getElementById('head-card-digit').innerText = headNumber;

   
})